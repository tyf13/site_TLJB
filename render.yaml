services:
  - type: web
    name: flask-dashboard
    env: python
    startCommand: "gunicorn app:app"
    buildCommand: "pip install -r requirements.txt"
    rootDir: "."     # <- ou supprime cette ligne
